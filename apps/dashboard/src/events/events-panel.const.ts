export const EVENT_STREAM_TAGS = "flex flex-wrap items-center gap-2" as const;

/** The stream is a single card, and like every other ledger on the page it scrolls inside itself. */
export const EVENT_STREAM = "list-none rounded-2xl border" as const;
