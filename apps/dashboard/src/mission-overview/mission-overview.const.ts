export const OVERVIEW = "grid gap-4" as const;

/** The goal is the page's own subject, so it is set on the page rather than inside a panel. */
export const MISSION = "flex flex-col gap-3 pt-2" as const;

export const GOAL_STATEMENT = "text-2xl leading-snug font-medium text-balance md:text-3xl" as const;

export const GOAL_REASON = "text-sm leading-relaxed text-muted-foreground" as const;

export const MISSION_META = "flex flex-wrap gap-x-6 gap-y-1 text-sm text-muted-foreground" as const;
