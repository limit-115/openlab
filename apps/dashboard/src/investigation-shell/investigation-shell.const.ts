/** What the investigation is, then what it has found, with the header standing clear of both. */
export const INVESTIGATION_PAGE = "grid gap-6" as const;

/**
 * A section is bounded by the space around it rather than by an outline, so the gap between two
 * sections has to stay clearly wider than the gap a section keeps between its heading and its cards.
 */
export const INVESTIGATION_DASHBOARD = "grid gap-10 text-sm" as const;
