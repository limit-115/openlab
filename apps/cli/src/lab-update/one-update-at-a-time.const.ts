/**
 * What an update in progress is called on disk.
 *
 * It sits in the program's home beside the receipt, so an operator who is told to delete it is told
 * to delete something they can see next to everything else this program keeps.
 */
export const UPDATE_LOCK_FILE = "update.lock";
