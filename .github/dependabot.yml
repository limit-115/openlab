version: 2

# Only actions are watched. Dependabot cannot update pnpm-lock.yaml for a dependency held by a
# workspace package, so its npm pull requests fail --frozen-lockfile and cannot merge.
updates:
    - package-ecosystem: github-actions
      directory: /
      schedule:
          interval: weekly
      groups:
          actions:
              patterns: ["*"]
